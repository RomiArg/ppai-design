package dsi.entrega3.repositories;

import dsi.entrega3.models.RespuestaDeCliente;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RespuestaDeClienteRepository extends JpaRepository<RespuestaDeCliente, Long> {
}
