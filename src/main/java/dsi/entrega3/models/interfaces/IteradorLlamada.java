package dsi.entrega3.models.interfaces;

import dsi.entrega3.models.Llamada;

// Hereda de Iterador los metodos y declara los tipos de datos/entidades que debe recibir y recorrer
public interface IteradorLlamada extends Iterador<Llamada, Object>{
}
